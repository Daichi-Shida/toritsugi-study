# 登録販売者資格試験 勉強アプリ — プロジェクト概要

## プロダクトビジョン
登録販売者資格試験合格を目指す女性向けのスマホ対応学習アプリ。
過去問を繰り返し解きながら、キャラクター成長システムで合格期待値を可視化する。
最終的には App Store / Google Play で有料（数百円・買い切り）として公開する。

## ターゲットユーザー
- 登録販売者資格試験を受験予定の女性
- スマホでスキマ時間に勉強したい層
- ゲーム的な達成感・モチベーション維持が必要な層

## コア機能（MVP）
1. **過去問クイズ** — 選択式・1問1答形式
2. **弱点集中モード** — 間違えた問題を優先的に出題（SRS: Spaced Repetition System）
3. **キャラクター成長** — 正解率・学習量に応じてキャラのレベルや見た目が変化
4. **合格期待値ゲージ** — 学習進捗を「合格まで○%」で可視化
5. **問題バンク更新** — 問題データをサーバーから更新可能な仕組み

## 技術スタック
- **フロントエンド**: Next.js 14 + TypeScript + Tailwind CSS
- **PWA**: スマホブラウザでそのままインストール可能にする（テスト版）
- **アプリ化**: 将来的に Expo / Capacitor でラップして App Store 公開
- **DB（ローカル）**: IndexedDB / localStorage（オフライン対応）
- **問題データ**: JSON形式（初期）→ Supabase などクラウドDB（本番）

## 開発ワークフロー
このプロジェクトは **Planner → Generator → Evaluator** の3エージェント方式で開発する。

```
Planner   : 要件 → 詳細仕様書（何を作るか）
Generator : 仕様書 → 実装（どう作るか）
Evaluator : 実装 → テスト・品質評価（正しく動くか）
```

エージェントは `.claude/agents/` に定義済み。

## スプリント計画（初期）
| Sprint | 内容 |
|--------|------|
| 1 | プロジェクト初期化・問題データ構造設計・クイズ基本UI |
| 2 | SRS（弱点優先）アルゴリズム実装 |
| 3 | キャラクター成長システム・合格期待値ゲージ |
| 4 | PWA化・オフライン対応 |
| 5 | UI磨き・アニメーション・達成感演出 |
| 6 | 問題データ拡充・テスト・リリース準備 |

## ディレクトリ構成（予定）
```
toritsugi-study/
├── .claude/
│   └── agents/        # Planner / Generator / Evaluator
├── docs/              # 仕様書・設計書
├── src/
│   ├── app/           # Next.js App Router
│   ├── components/    # UIコンポーネント
│   ├── lib/           # ロジック（SRS, スコア計算など）
│   ├── data/          # 問題データJSON
│   └── types/         # TypeScript型定義
├── public/
└── CLAUDE.md
```

## 品質基準（Evaluator チェックリスト）
- [ ] スマホ（375px幅）で問題なく表示・操作できる
- [ ] 1問解答 → 次の問題へのフロー がスムーズ
- [ ] 間違えた問題が次回以降優先的に出題される
- [ ] キャラクターのレベル変化が視覚的にわかる
- [ ] オフライン状態でも基本機能が動く

## 現在地（2026-08-17）
- 出題プール **1321問**。全問が各実施団体の**公式PDF由来で本試験そのままの形式**
  （南関東は令和3〜7年度の5年分、他6ブロックは令和7年度、＋手引き令和8年4月改訂の自作30問）
- 詳しい経緯・PDF解析の落とし穴・検証コマンドは `docs/session_memory.md` を参照
- 問題を作り直すときは `python3 scripts/gen_official_questions.py` を使う。
  **`scripts/gen_kakomon_questions.py` は実行しないこと**（同じファイル名を出力して原文形式を壊す）
- 変更後は `python3 scripts/audit_questions.py`（公式PDFの正答と再照合する監査）を必ず通す
- 妻が実際に使用中。学習記録は `retired_index.json` と `src/lib/questionIndex.ts` で
  問題入れ替え後も章別正答率が動かないようにしてある
